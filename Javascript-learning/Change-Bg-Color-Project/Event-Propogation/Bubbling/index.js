// event - Bubbling
// event occuring in child is passed to parents if parent has defined event-listners for same event

// event - capturing
// we add a third argument to addEventListner("event", function, boolean -> true/false) to decide
// whether we should capture the event or not??

const granparent = document.querySelector